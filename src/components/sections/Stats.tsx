export default function Stats() {
  return (
    <section className="section" id="stats">
      <div className="section-shell">
        <div className="glass card edge-glint fade-in-up px-6 py-8 sm:px-8 sm:py-10 lg:px-10">
          <p className="text-sm font-medium uppercase tracking-[0.12em] text-[var(--text-dim)]">Tény</p>
          <h2 className="mt-3 text-2xl font-semibold leading-tight tracking-tight sm:text-3xl">
            A WHO szerint hetente <span className="highlight-text">150 perc</span> mozgás szükséges.
          </h2>
        </div>
      </div>
    </section>
  );
}
